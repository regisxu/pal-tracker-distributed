apply from: "$projectDir/../server.gradle"

dependencies {
    compile project(":components:timesheets")
    compile "io.pivotal.spring.cloud:spring-cloud-services-starter-circuit-breaker:1.5.0.RELEASE"
}
